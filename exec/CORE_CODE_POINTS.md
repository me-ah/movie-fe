# [발표자료] MEAH 백엔드 핵심 코드 포인트 (Top 5)

이 문서는 발표 시 코드를 직접 보여주며 기술적 깊이를 설명하기 위한 가이드입니다.

---

## 1. 실시간 장르 선호도 누적 시스템
- **파일 경로:** `accounts/models.py`
- **코드 위치:** `UserMovieHistory` 클래스의 `save()` 메서드 (약 70 ~ 85라인)
- **설명 포인트:** 
    - 사용자의 영상 시청 행동을 감지하는 즉시 DB 레벨에서 장르 점수를 자동 산출합니다.
    - 단순히 "봤다"는 사실이 아니라 **"시청 시간(watch_time)"**을 가중치로 활용하여 취향 데이터의 정밀도를 높였습니다.

## 2. 혼합 장르 평균 점수 산정 (Averaging)
- **파일 경로:** `home/views.py`
- **코드 위치:** `SubView` 클래스의 점수 계산 루프 (약 55 ~ 75라인)
- **설명 포인트:** 
    - 471개의 세부 카테고리 랭킹을 매길 때, 혼합 장르(예: 액션+스릴러)의 점수를 각 장르 점수의 **산술 평균**으로 계산합니다.
    - 이를 통해 유저의 복합적인 취향을 수학적으로 정확하게 큐레이션에 반영합니다.

## 3. 6:2:2 하이브리드 추천 믹스 엔진
- **파일 경로:** `movies/recommendation.py`
- **코드 위치:** `generate_personalized_playlist()` 함수 (약 55 ~ 75라인)
- **설명 포인트:** 
    - 유저 취향(60%), 새로운 장르 탐색(20%), 트렌드 반영(20%)이라는 황금 비율을 코드로 구현했습니다.
    - **라운드-로빈(Round-Robin)** 수집 방식을 통해 특정 장르 편향을 막고 다양성을 확보한 핵심 알고리즘입니다.

## 4. Redis 기반 선제적 쇼츠 충전 (Proactive Buffering)
- **파일 경로:** `movies/views.py`
- **코드 위치:** `get_shorts_list()` 함수 (약 25 ~ 45라인)
- **설명 포인트:** 
    - **Redis 인메모리 데이터베이스**를 활용하여 유저별 플레이리스트를 관리합니다.
    - 유저가 콘텐츠 끝에 도달하기 전 백그라운드에서 다음 배치를 미리 생성하여, 무한 스크롤 시 **로딩 지연(Latency)이 없는** 사용자 경험을 제공합니다.

## 5. 동적 카테고리 자동 생성 (Auto-Categorization)
- **파일 경로:** `home/management/commands/refresh_home.py`
- **코드 위치:** `handle()` 메서드 내 조합 분석 로직 (약 35 ~ 60라인)
- **설명 포인트:** 
    - 하드코딩된 카테고리가 아닌, 실제 DB 데이터를 전수 조사하여 유효한 장르 조합을 스스로 찾아내 카테고리화하는 **데이터 주도형(Data-driven)** 로직입니다.

---

### 💡 발표 팁
> "저희 백엔드는 단순히 데이터를 서빙하는 수준을 넘어, **사용자의 시청 시간을 모델 레벨(1번)에서 정밀하게 분석**하고, 이를 **하이브리드 엔진(3번)**과 **Redis 버퍼링(4번)** 기술로 연결하여 개인화된 경험을 실시간으로 구현했다는 점이 가장 큰 기술적 성과입니다."
